# Directory Structure
* setpoint_generator.py is an automated script that automatically runs the setpoint generator and saves the result as a txt file
* main.py is the fuzzing module

# How to use
* put the two files at the same directory path
* write PLANS in main.py,
* run main.py